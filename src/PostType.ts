export class Post {
  heading: string;
  text: string;
  link: string;
  constructor(heading: string, text: string, link: string) {
    this.heading = heading;
    this.text = text;
    this.link = link;
  }
}
